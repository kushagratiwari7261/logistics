[build]
# Tell Railway to use the Dockerfile in this folder
builder = "dockerfile"
dockerfilePath = "backend-python/Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
